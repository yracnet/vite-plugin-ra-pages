import { AccountBox } from "@mui/icons-material";
import { ListGuesser } from "react-admin";

export default () => <ListGuesser />;

export const Icon = AccountBox;

export const Options = {
  label: "User Index",
};

export const Roles = [];
